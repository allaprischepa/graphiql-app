export enum AppRoutes {
  notFound = '/*',
  layout = '/',
  welcome = '/welcome',
  main = '/main',
  signIn = '/sign-in',
  signUp = '/sign-up',
}

export enum LangActionType {
  SET_LANGUAGE = 'SET_LANGUAGE',
  UNKNOWN = 'UNKNOWN',
}

export enum Languages {
  EN = 'en',
  RU = 'ru',
}
